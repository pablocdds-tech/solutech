fix: refatorar modulo CD->Loja como venda real com linhas de itens

- FIX BUG CRITICO: newSourceIndex -> newSourceStoreId (pedido nunca era criado)
- Tabela de pedidos agora mostra: data, origem, destino, status, qtd itens, total
- Detalhe do pedido mostra nomes das lojas (origem/destino) em vez de UUIDs
- Formulario de itens: busca preco automatico da tabela item_prices por loja destino
- Edicao inline de quantidade (clique na qty para editar)
- Botao de cancelar pedido adicionado
- Actions refatoradas: listInternalOrders e getInternalOrder resolvem nomes de lojas
- Nova action: updateInternalOrderItem, cancelInternalOrder, getItemPriceForStore
- Items resolvem nomes via join em getInternalOrderItems
- Seed de teste: 2 itens extras, precos para NB e Praca, 1 pedido rascunho com 2 itens
